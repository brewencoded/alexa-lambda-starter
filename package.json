{
  "name": "alexa",
  "version": "1.0.0",
  "description": "alexa",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .; exit 0",
    "test": "npm run lint && mocha --recursive --reporter spec test/**/*.spec.js; exit 0"
  },
  "author": "Victor Rodriguez",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.18.0",
    "gulp": "^3.9.1",
    "mocha": "^3.2.0",
    "sinon": "^2.1.0"
  },
  "dependencies": {
    "joi": "^10.3.1"
  }
}
